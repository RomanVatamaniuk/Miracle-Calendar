import { GetDayNamePipe } from './get-day-name.pipe';

describe('GetDayNamePipe', () => {
  it('create an instance', () => {
    const pipe = new GetDayNamePipe();
    expect(pipe).toBeTruthy();
  });
});
